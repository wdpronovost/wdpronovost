# Signal Garden v1 — local archive

Snapshot of the production prototype found in `dist/` immediately before the Signal Garden V2 rebuild on 2026-07-14.

This directory is intentionally outside `dist/` and is not published by Netlify. It preserves the prior HTML, CSS, and JavaScript as a local design reference.
