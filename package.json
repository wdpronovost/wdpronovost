{
  "name": "signal-garden",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "lint": "node scripts/verify.mjs",
    "test": "npm run build && node --test tests/*.spec.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
